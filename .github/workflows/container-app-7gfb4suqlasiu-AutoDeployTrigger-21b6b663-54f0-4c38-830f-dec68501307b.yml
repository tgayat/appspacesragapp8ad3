name: Trigger auto deployment for container-app-7gfb4suqlasiu

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-7gfb4suqlasiu-AutoDeployTrigger-21b6b663-54f0-4c38-830f-dec68501307b.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPP7GFB4SUQLASIU_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPP7GFB4SUQLASIU_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPP7GFB4SUQLASIU_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPP7GFB4SUQLASIU_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPP7GFB4SUQLASIU_REGISTRY_PASSWORD }}
          containerAppName: container-app-7gfb4suqlasiu
          resourceGroup: appspacesragapp8ad389b0
          imageToBuild: default/container-app-7gfb4suqlasiu:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


